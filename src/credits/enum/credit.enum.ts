export enum CreditStatus {
    PENDING = 1, // Credit application is pending.
    APPROVED = 2, // Credit application approved.
    REJECTED = 3, // Credit application rejected.
    PAID = 4, // Credit debt has been paid.
    DEFAULTED = 5 // Credit payment not made.
}